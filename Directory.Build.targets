<Project>

  <!-- Intentionally minimal. the repo is self-contained and deliberately keeps this minimal;
       repo-root Directory.Build.targets (which stages the old tree's WinNatives). Per-module native
       deployment (PortAudio, PortMidi, SDL3, FFmpeg, NDI, libass, …) is added in the phase that
       introduces the native dependency — Phase 2 onward. -->

</Project>
