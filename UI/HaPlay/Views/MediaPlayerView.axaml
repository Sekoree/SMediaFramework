<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:HaPlay.ViewModels"
             xmlns:views="using:HaPlay.Views"
             mc:Ignorable="d" d:DesignWidth="780" d:DesignHeight="900"
             x:Class="HaPlay.Views.MediaPlayerView"
             x:DataType="vm:MediaPlayerViewModel">

  <UserControl.Resources>
    <views:FilenameConverter x:Key="FilenameConverter" />
  </UserControl.Resources>

  <UserControl.Styles>
    <Style Selector="TextBlock.section-title">
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Margin" Value="0,0,0,4" />
    </Style>
    <Style Selector="TextBlock.time">
      <Setter Property="FontFamily" Value="Consolas,monospace" />
      <Setter Property="FontSize" Value="13" />
    </Style>
    <Style Selector="TextBlock.time-label">
      <Setter Property="FontSize" Value="11" />
      <Setter Property="Opacity" Value="0.6" />
      <Setter Property="TextAlignment" Value="Center" />
    </Style>
    <Style Selector="Border.card">
      <Setter Property="BorderBrush" Value="#22808080" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="6" />
      <Setter Property="Padding" Value="14" />
      <Setter Property="Background" Value="#08808080" />
    </Style>
    <Style Selector="Button.transport">
      <Setter Property="MinWidth" Value="56" />
      <Setter Property="Padding" Value="12,8" />
    </Style>
    <Style Selector="Button.play-pause">
      <Setter Property="MinWidth" Value="96" />
      <Setter Property="Padding" Value="14,8" />
      <Setter Property="FontWeight" Value="SemiBold" />
    </Style>
  </UserControl.Styles>

  <ScrollViewer HorizontalScrollBarVisibility="Disabled" Padding="14">
    <DockPanel LastChildFill="True">

      <!-- Status banner -->
      <Border DockPanel.Dock="Top"
              IsVisible="{Binding StatusMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
              Background="#33C00000" BorderBrush="#88C00000" BorderThickness="1"
              CornerRadius="4" Padding="10,6" Margin="0,0,0,12">
        <TextBlock Text="{Binding StatusMessage}" Foreground="#FFE6E6" TextWrapping="Wrap" />
      </Border>

      <StackPanel Spacing="12">

        <!-- Now-playing / transport card -->
        <Border Classes="card">
          <StackPanel Spacing="12">

            <!-- Header: player name + settings flyout -->
            <Grid ColumnDefinitions="*,Auto">
              <TextBlock Grid.Column="0" FontWeight="SemiBold" FontSize="16"
                         VerticalAlignment="Center" Text="{Binding Name}" />
              <Button Grid.Column="1" Padding="8,4" Content="⚙ Settings"
                      ToolTip.Tip="Save / load this player's configuration, or remove the player.">
                <Button.Flyout>
                  <MenuFlyout>
                    <MenuItem Header="Save player config…" Command="{Binding SavePlayerConfigCommand}" />
                    <MenuItem Header="Load player config…" Command="{Binding LoadPlayerConfigCommand}" />
                    <Separator />
                    <MenuItem Header="Unload current session"
                              Command="{Binding CloseSessionCommand}" />
                    <MenuItem Header="Remove this player"
                              Command="{Binding RemovePlayerCommand}"
                              IsVisible="{Binding CanRemove}" />
                  </MenuFlyout>
                </Button.Flyout>
              </Button>
            </Grid>

            <!-- Current source -->
            <StackPanel Orientation="Horizontal" Spacing="8">
              <Border Background="#22808080" CornerRadius="3" Padding="6,2"
                      VerticalAlignment="Center"
                      ToolTip.Tip="What the loaded source contains.">
                <TextBlock FontSize="11" FontWeight="SemiBold"
                           Text="{Binding SourceKindLabel}" />
              </Border>
              <TextBlock Opacity="0.75" FontSize="12" TextTrimming="CharacterEllipsis"
                         VerticalAlignment="Center"
                         Text="{Binding MediaFilePath, FallbackValue='No media loaded — pick a file from the playlist below.', TargetNullValue='No media loaded — pick a file from the playlist below.'}"
                         ToolTip.Tip="{Binding MediaFilePath}" />
            </StackPanel>

            <!-- Scrubber: TwoWay binding for visual tracking; commit on PointerReleased (code-behind) -->
            <Slider Name="SeekSlider"
                    Minimum="0" Maximum="1000"
                    Value="{Binding SeekSliderValue, Mode=TwoWay}"
                    IsEnabled="{Binding IsMediaLoaded}" />

            <!-- Three times: position / remaining / total -->
            <Grid ColumnDefinitions="*,*,*">
              <StackPanel Grid.Column="0">
                <TextBlock Classes="time" HorizontalAlignment="Center"
                           Text="{Binding CurrentPositionText}" />
                <TextBlock Classes="time-label" Text="Position" />
              </StackPanel>
              <StackPanel Grid.Column="1">
                <TextBlock Classes="time" HorizontalAlignment="Center"
                           Text="{Binding RemainingTimeText}" />
                <TextBlock Classes="time-label" Text="Remaining" />
              </StackPanel>
              <StackPanel Grid.Column="2">
                <TextBlock Classes="time" HorizontalAlignment="Center" Opacity="0.75"
                           Text="{Binding DurationText}" />
                <TextBlock Classes="time-label" Text="Duration" />
              </StackPanel>
            </Grid>

            <!-- Transport row: prev / play-pause / next / stop + loop + display-mode -->
            <Grid ColumnDefinitions="*,Auto,*">
              <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6"
                          HorizontalAlignment="Center">
                <Button Classes="transport" Content="⏮"
                        Command="{Binding PreviousTrackCommand}"
                        ToolTip.Tip="Previous track" />
                <Button Classes="play-pause"
                        Content="{Binding PlayPauseLabel}"
                        Command="{Binding TogglePlayPauseCommand}"
                        ToolTip.Tip="Play or pause the current track." />
                <Button Classes="transport" Content="⏭"
                        Command="{Binding NextTrackCommand}"
                        ToolTip.Tip="Next track" />
                <Button Classes="transport" Content="⏹"
                        Command="{Binding StopCommand}"
                        ToolTip.Tip="Stop and rewind to 00:00." />
              </StackPanel>
              <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6"
                          HorizontalAlignment="Right">
                <ToggleButton Content="🔁 Loop" IsChecked="{Binding IsLooping}" Padding="10,6"
                              ToolTip.Tip="Restart the current track when it ends." />
                <ToggleButton Content="⏭ Auto-advance"
                              IsChecked="{Binding AutoAdvancePlaylist}" Padding="10,6"
                              ToolTip.Tip="When on, the next playlist entry loads and plays automatically when the current track ends." />
              </StackPanel>
            </Grid>
          </StackPanel>
        </Border>

        <!-- Playlist card -->
        <Border Classes="card"
                ToolTip.Tip="Double-click a row to load + play. Single-click selects so Play loads that file.">
          <DockPanel LastChildFill="True">
            <Grid DockPanel.Dock="Top" ColumnDefinitions="*,Auto,Auto,Auto,Auto" Margin="0,0,0,8" ColumnSpacing="4">
              <TextBlock Grid.Column="0" Text="Playlist" Classes="section-title" VerticalAlignment="Center" />
              <Button Grid.Column="1" Content="↑" Command="{Binding MovePlaylistItemUpCommand}"
                      MinWidth="34" ToolTip.Tip="Move the selected entry up." />
              <Button Grid.Column="2" Content="↓" Command="{Binding MovePlaylistItemDownCommand}"
                      MinWidth="34" ToolTip.Tip="Move the selected entry down." />
              <Button Grid.Column="3" Content="Remove" Command="{Binding RemoveFromPlaylistCommand}"
                      ToolTip.Tip="Remove the selected entry." />
              <Button Grid.Column="4" Content="+ Add files…"
                      Command="{Binding AddFilesToPlaylistCommand}" />
            </Grid>

            <ListBox Name="PlaylistListBox" MinHeight="220" MaxHeight="380"
                     ItemsSource="{Binding PlaylistPaths}"
                     SelectedItem="{Binding SelectedPlaylistPath, Mode=TwoWay}"
                     DoubleTapped="OnPlaylistItemDoubleTapped">
              <ListBox.ItemTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding Converter={StaticResource FilenameConverter}}"
                             ToolTip.Tip="{Binding}"
                             TextTrimming="CharacterEllipsis" />
                </DataTemplate>
              </ListBox.ItemTemplate>
            </ListBox>
          </DockPanel>
        </Border>

        <!-- Routing expander (collapsed once configured; shows count when collapsed) -->
        <Expander IsExpanded="{Binding IsRoutingExpanded, Mode=TwoWay}">
          <Expander.Header>
            <StackPanel Orientation="Horizontal" Spacing="8">
              <TextBlock Text="Routing" FontWeight="SemiBold" />
              <TextBlock Opacity="0.7" Text="{Binding RoutingSummary}" />
            </StackPanel>
          </Expander.Header>
          <Border Classes="card" Padding="10">
            <StackPanel Spacing="6">
              <Grid ColumnDefinitions="*,Auto">
                <TextBlock Grid.Column="0" Opacity="0.7" FontSize="12"
                           Text="Tick the outputs this player should drive." />
                <Button Grid.Column="1" Content="+ Add output…" Padding="10,4">
                  <Button.Flyout>
                    <MenuFlyout>
                      <MenuItem Header="PortAudio output…"
                                Command="{Binding AddPortAudioOutputCommand}" />
                      <MenuItem Header="Local video output…"
                                Command="{Binding AddLocalVideoOutputCommand}" />
                      <MenuItem Header="NDI output…"
                                Command="{Binding AddNDIOutputCommand}" />
                    </MenuFlyout>
                  </Button.Flyout>
                </Button>
              </Grid>
              <ItemsControl ItemsSource="{Binding Outputs}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate x:DataType="vm:PlayerOutputBinding">
                    <Grid ColumnDefinitions="Auto,*" Margin="0,2">
                      <CheckBox Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"
                                IsChecked="{Binding IsSelected}"
                                IsEnabled="{Binding Line.SupportsMediaPlayerRouting}" />
                      <StackPanel Grid.Column="1" Spacing="1"
                                  ToolTip.Tip="{Binding Line.Summary}">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                          <TextBlock FontWeight="SemiBold" Text="{Binding Line.KindLabel}" />
                          <TextBlock Text="—" Opacity="0.5" />
                          <TextBlock Text="{Binding Line.Definition.DisplayName}" />
                        </StackPanel>
                      </StackPanel>
                    </Grid>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
              <TextBlock Opacity="0.55" FontSize="11"
                         IsVisible="{Binding HasNoOutputs}"
                         Text="No outputs configured yet — add one above. Click Play with nothing selected to auto-route." />
            </StackPanel>
          </Border>
        </Expander>

        <!-- Hold-image / display-mode expander -->
        <Expander
                  ToolTip.Tip="Show a still image on every video output instead of decoded pixels — outputs are resized to the image's native dimensions when set.">
          <Expander.Header>
            <StackPanel Orientation="Horizontal" Spacing="8">
              <TextBlock Text="Hold image" FontWeight="SemiBold" />
              <TextBlock Opacity="0.7" Text="{Binding HoldImageSummary}" />
            </StackPanel>
          </Expander.Header>
          <Border Classes="card" Padding="10">
            <StackPanel Spacing="8">
              <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                <TextBox Grid.Column="0" PlaceholderText="PNG / JPEG path…"
                         Text="{Binding FallbackImagePath}" />
                <Button Grid.Column="1" Content="Browse…"
                        Command="{Binding BrowseFallbackCommand}" />
              </Grid>
              <ToggleButton Content="Show hold image on video outputs"
                            IsChecked="{Binding HoldFallbackVideo}"
                            HorizontalAlignment="Left"
                            ToolTip.Tip="When on, video outputs resize to the image and display it instead of decoded frames." />
            </StackPanel>
          </Border>
        </Expander>

      </StackPanel>
    </DockPanel>
  </ScrollViewer>
</UserControl>
