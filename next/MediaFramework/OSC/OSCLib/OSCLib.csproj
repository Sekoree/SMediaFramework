<Project Sdk="Microsoft.NET.Sdk">

    <!-- OSCLib — Open Sound Control over UDP: client/server, address-pattern matching, bundle/time-tag scheduling,
         packet codec. The OSC transport for S.Control (the analogue of PMLib for MIDI); bridges inbound OSC to
         Core's TriggerBus. Pure managed; common build settings come from next/Directory.Build.props. -->

    <ItemGroup>
        <InternalsVisibleTo Include="OSCLib.Tests" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="../../Media/S.Media.Core/S.Media.Core.csproj" />
    </ItemGroup>

</Project>
