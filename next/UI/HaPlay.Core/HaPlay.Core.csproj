<Project Sdk="Microsoft.NET.Sdk">

    <!-- HaPlay.Core (Phase 8): the UI-agnostic MVVM spine — view models over the headless ShowSession, no Avalonia.
         The HaPlay app (HaPlay.App/.Desktop) binds to these; tests drive them without a UI. Not part of the AOT
         framework (the UI tier isn't AOT-published). -->

    <PropertyGroup>
        <IsAotCompatible>false</IsAotCompatible>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="../../MediaFramework/Media/S.Media.Session/S.Media.Session.csproj" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="CommunityToolkit.Mvvm" />
    </ItemGroup>

</Project>
