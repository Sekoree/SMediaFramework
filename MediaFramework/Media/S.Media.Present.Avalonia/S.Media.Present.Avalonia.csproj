<Project Sdk="Microsoft.NET.Sdk">

    <!-- S.Media.Present.Avalonia - IVideoOutput as an Avalonia OpenGlControlBase, sharing S.Media.Gpu's
         YuvVideoRenderer with the SDL3 presenter. Output-only ([Core, Gpu]); zero-copy external-image
         import (IGlContextExternalObjectsFeature) is the live-slice refinement, CPU re-upload otherwise. -->

    <ItemGroup>
        <PackageReference Include="Avalonia" />
        <PackageReference Include="Silk.NET.OpenGL" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="../S.Media.Core/S.Media.Core.csproj" />
        <ProjectReference Include="../S.Media.Gpu/S.Media.Gpu.csproj" />
    </ItemGroup>


    <PropertyGroup>
        <Description>Embed MFPlayer video into Avalonia applications (the framework's UI presentation seam).</Description>
    </PropertyGroup>
</Project>
