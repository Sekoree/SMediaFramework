<Project Sdk="Microsoft.NET.Sdk">

    <!-- Phase 3: verifies CompositeMulti's typed targets on real GL — zero-copy GlCompositeTarget (blit into
         a caller-owned FBO) and CpuFrameCompositeTarget (readback) from one composite. Owns its SDL GL
         context and drives GlVideoCompositor directly. No FFmpeg. -->

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <IsAotCompatible>false</IsAotCompatible>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="../../Media/S.Media.Compositor/S.Media.Compositor.csproj" />
        <ProjectReference Include="../../Media/S.Media.Present.SDL3/S.Media.Present.SDL3.csproj" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Silk.NET.OpenGL" />
    </ItemGroup>

</Project>
