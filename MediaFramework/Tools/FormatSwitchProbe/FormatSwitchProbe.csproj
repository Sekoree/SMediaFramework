<Project Sdk="Microsoft.NET.Sdk">

    <!-- Phase 3 gate: mid-stream video format-change reconfig. Submits BGRA frames of one resolution
         through a VideoRouter, then switches resolution mid-stream, and asserts the downstream output
         re-Configures (idempotent reconfigure path - memory: video_sink_pump_reconfigure). No FFmpeg. -->

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <IsAotCompatible>false</IsAotCompatible>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="../../Media/S.Media.Routing/S.Media.Routing.csproj" />
    </ItemGroup>

</Project>
